{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ideja</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ideja.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ ideja.text }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ideja.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ideja_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ideja_edit', { 'id': ideja.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
