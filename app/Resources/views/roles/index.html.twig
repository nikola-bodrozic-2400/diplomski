{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lista rola</h1>

    <table class='table table-bordered table-striped table-hover'>
        <thead>
            <tr>
                <th>Id</th>
                <th>Ime</th>
                <th>Može da čita</th>
                <th>Može da čita i piše</th>
                <th>Akcija</th>
            </tr>
        </thead>
        <tbody>
        {% for role in roles %}
            <tr>
                <td><a href="{{ path('roles_show', { 'id': role.id }) }}">{{ role.id }}</a></td>
                <td>{{ role.name }}</td>
                <td>{% if role.canRead %}Yes{% else %}No{% endif %}</td>
                <td>{% if role.canReadWrite %}Yes{% else %}No{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('roles_show', { 'id': role.id }) }}">prikaži</a>
                        </li>
                        <li>
                            <a href="{{ path('roles_edit', { 'id': role.id }) }}">edituj</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('roles_new') }}">Napravi novu rolu</a>
        </li>
    </ul>
{% endblock %}
